# Mismo proyecto de Supabase que ya usa la app de Expo
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Clave pública VAPID para Web Push (generarla con: npx web-push generate-vapid-keys)
# La clave PRIVADA va en la Edge Function del lado del servidor, nunca acá.
VITE_VAPID_PUBLIC_KEY=
